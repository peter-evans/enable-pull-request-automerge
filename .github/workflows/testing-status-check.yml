name: Testing status check
on:
  pull_request:
    branches:
      - 'ci-test/built'
      - 'ci-test/committed'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing status check"
